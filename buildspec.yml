version: 0.2

phases:
  install:
    commands:
      - ansible --version
      - packer --version
      - aws --version
  pre_build:
    commands:
      - /bin/sh scripts/setup.sh
  build:
    commands:
      - echo "$ANSIBLE_VAULT_PASS" > vault_password_file
      - /bin/sh scripts/run.sh
